<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifro.vilhena.abastecimentosv2">

    <application
        android:allowBackup="true"
        android:icon="@drawable/combustivel"
        android:label="@string/app_name"
        android:roundIcon="@drawable/combustivel"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListarCombustivelActivity">
        </activity>
        <activity android:name=".CombustivelActivity"></activity>
        <activity android:name=".ResumoActivity"></activity>
        <activity android:name=".AbastecimentoActivity" ></activity>
        <activity android:name=".ListarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>